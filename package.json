{
  "name": "pause-for-thought-website",
  "version": "0.0.1",
  "scripts": {
    "start": "nodemon lib/application.js",
    "wbuild": "nodemon --watch src --exec npm run build",
    "build": "babel src -d lib"
  },
  "dependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "express": "^4.13.4"
  }
}
